# skypro_final_task_auto
Дипломная работа. Архитектура фреймворка

Стек

    pytest
    selenium
    request
    allure

Библиотеки

    pip install pytest
    pip install selenium
    pip install webdriver-manager
    pip install allure
